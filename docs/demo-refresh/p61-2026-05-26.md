---
name: p61-demo-refresh-2026-05-26
description: P61 (61-GovOps / agentic-state-govops-lac) HF Space health check and refresh decision for the ESDC showcase window. Stage 1 of the p61-demo-refresh-and-azure-parity contract.
metadata:
  type: report
  contract: p61-demo-refresh-and-azure-parity
  stage: 1
  date: 2026-05-26
---

# P61 demo refresh — 2026-05-26

Stage 1 of `81-AIOS/contracts/p61-demo-refresh-and-azure-parity.contract.md`.
Stage 2 (Azure mirror) NOT executed — `STAGE_2_AZURE_MIRROR` flag was not set in the dispatch.

## 1. Initial health check

URL: `https://agentic-state-govops-lac.hf.space/`

| Metric          | Value                                  |
|-----------------|----------------------------------------|
| HTTP code       | 200                                    |
| Total latency   | 0.361 s                                |
| Bytes downloaded| 9 641                                  |
| Check timestamp | 2026-05-26 (UTC, dispatch session)     |

Command:

```
curl -fsS -o NUL -w "%{http_code} %{time_total} %{size_download}" \
  "https://agentic-state-govops-lac.hf.space/"
```

## 2. DOM / visible-elements check

Headless `curl` of the landing HTML confirms the SPA shell renders with the expected GovOps chrome:

- `<title>` = `GovOps — Law as code, with provenance you can read`
- Header (`<header role="banner">`) with `Gov0ps` wordmark
- Primary nav (`<nav aria-label="Primary">`): Home / Walkthrough / Authority / About / Console
- Global jurisdiction switcher (`data-testid="global-jurisdiction-switcher"`) with CA selected by default and BR/ES/FR/DE/UA/JP options
- Language switcher (lucide `languages` icon + radix combobox)
- Theme toggle (`Toggle theme` button with `lucide-moon` icon + "Light" label)
- Skip-to-content link (`<a href="#main">`) for keyboard / SR users
- Footer `govops · spec govops-008`
- TanStack Router boot script + Vite asset preloads (`/assets/index-Ce6fditG.js`, `/assets/styles-Cr8qXyR0.css`, `ProvenanceRibbon-*.js`, `ValueTypeBadge-*.js`, `JurisdictionChip-*.js`)

The page is the production SPA (Vite-built, served by the v2.1 single-container `Dockerfile`); no error pages, no Space-cold-start placeholder, no broken-asset warnings in the served HTML.

## 3. Repo / dependency staleness review

Source `61-GovOps` git state at scan time:

- Origin: `https://github.com/agentic-state/GovOps-LaC.git` (public-tier per [[feedback_public_repo_two_tier_layout]])
- HF remote: `https://huggingface.co/spaces/agentic-state/govops-lac` (auto-deploys on push)
- Most recent main commits (top 5):

  ```
  9b9c2d6  deps(web)(deps): bump the react-intl group in /web with 2 updates (#80)
  2fabc0a  chore(repo-hygiene): close PLAN residue + IDEA-v3.2 line + ADR range (audit-of-audit) (#78)
  9562c63  chore(repo-hygiene): public-tier doctrine + close 10 cross-ledger discrepancies (#77)
  4177f8f  docs: meta-audit corrections (line-anchor + stale test counts + plan refs) (#76)
  6f02391  docs+policies: clear broken-link drift (14 -> 0) (#75)
  ```

Dependabot is active (PR #80 landed today), CodeQL + drift hygiene are current, no obviously-stale or CVE'd dependency line was visible in `web/package.json` (Radix UI, React Hook Form, CodeMirror, Cloudflare Vite plugin all on current minor lines).

## 4. Refresh decision

**No refresh needed.**

- HF Space returns HTTP 200 with sub-400 ms TTFB
- The served HTML reflects the current production SPA build
- Origin main is at the freshly-merged Dependabot bump (`9b9c2d6`); HF auto-deploys from the linked Space, so the live image tracks main
- No CVE / stale-dep finding surfaced in the manifest review

A defensive force-rebuild push to `hf` was considered and rejected: there is no observable defect to fix, and a no-op push would (a) consume HF build minutes and (b) briefly recycle the running container during a window where the ESDC showcase URL needs to stay green.

## 5. Post-refresh health

N/A — no refresh performed.

## 6. Cost

HF Spaces free tier — $0 incurred. No additional cost accrued by this dispatch.

## 7. AIOS drift scan

Run from the worktree root:

```
$ python -c "import sys; sys.path.insert(0,'c:/eva-foundry/81-AIOS/src'); \
            from aios.cli import main; main(['drift','scan'])"
AIOS drift scan -- repo=C:\eva-foundry\eva-foundation\.claude\worktrees\p61-demo-refresh-2026-05-26
  Generated at:       2026-05-26T11:55:04.950886+00:00
  Schema:             aios.integrity.v1
  Finding count:      0
  Highest severity:   none
  write_safe:         0
```

Clean (0 findings).

## 8. Stage 2 (Azure mirror) — not executed

`STAGE_2_AZURE_MIRROR` was not set to `true` in the dispatch prompt, so the optional Azure mirror is skipped. The HF Space remains the single showcase surface for P61 during this window. PO can elect Stage 2 in a follow-on dispatch if single-cloud parity with P53 becomes desired.

## 9. Acceptance gates (Stage 1)

| Gate                                                          | Evidence in this report |
|---------------------------------------------------------------|-------------------------|
| HF Space health check completed                               | §1 (HTTP 200, 0.361 s)  |
| Refresh decision documented                                   | §4 (no-refresh-needed)  |
| If refreshed: post-refresh health green                       | §5 (N/A — no refresh)   |
| `aios drift scan` clean on 61-GovOps                          | §7 (0 findings)         |
| Report covers all required sections (health / decision / cost)| §1-§7                   |
